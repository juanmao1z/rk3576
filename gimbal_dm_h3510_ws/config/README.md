# Gimbal Config

这里预留 DM-H3510 的运行参数。

建议后续记录：

- 通信方式：串口、CAN、PWM 或厂商 SDK。
- 设备路径：例如 `/dev/ttyUSB0`、`/dev/ttyS*`、`can0`。
- 波特率或 CAN bitrate。
- pan/tilt 方向、角度限位、速度限位。
- 归中位置、死区、跟踪 PID 参数。
- 丢目标后的动作：保持、回中或扫描。
